import { NombreDirectiva } from './nombre-directiva';

describe('NombreDirectiva', () => {
  it('should create an instance', () => {
    const directive = new NombreDirectiva();
    expect(directive).toBeTruthy();
  });
});
